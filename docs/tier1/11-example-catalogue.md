<!-- Generated by docs/tier1/build.py. Do not edit by hand; run `make tier1`. -->

# 11 — Example catalogue

*Derived from `examples/**/*.py`.*

79 runnable scripts. `make examples` lists them interactively; `make examples-check` runs every one and fails when any stops working.

## `examples/`

| Script | Demonstrates |
|---|---|
| `main.py` | Example Launcher |

## `examples/src/`

| Script | Demonstrates |
|---|---|
| `config.py` | Example settings loaded from .env via pydantic-settings. |
| `dataset.py` | Slices of the Costa Rica delivery dataset — `data/deliveries_cr.json`. |
| `maps.py` | Folium helpers for the examples that draw. |

## `examples/src/benchmarking/`

| Script | Demonstrates |
|---|---|
| `compare_tsp.py` |  |
| `published_instances.py` | What the engine scores on problems other people have already solved. |

## `examples/src/clustering/`

| Script | Demonstrates |
|---|---|
| `generate_payload.py` | The payload the other clustering examples post, built from the real corpus. |
| `run_clustering_workflow.py` |  |
| `simple_id_example.py` |  |

## `examples/src/fleet/`

| Script | Demonstrates |
|---|---|
| `clustering_mode_comparison.py` | VRP Clustering Mode Comparison |
| `generate_delivery_dataset.py` | Generate a planned-delivery dataset for Costa Rica, snapped to the road network. |
| `hysteresis_demo.py` | Hysteresis Buffer Demo |
| `objective_modes.py` | Is this delivery worth driving to? Ask the objective, not the solver. |
| `stress_test_vrp.py` |  |
| `verify_delivery_plan.py` | Build a delivery plan from real data, evaluate it, and verify it independently. |
| `visualize_vrp.py` |  |

## `examples/src/fleet/adversarial/`

| Script | Demonstrates |
|---|---|
| `pathological_instances.py` | Fifteen instances that break routing engines, and what this one answers. |

## `examples/src/fleet/alloc/`

| Script | Demonstrates |
|---|---|
| `depot_inventory.py` | A depot is not a spring. |
| `fleet_minimisation.py` | The route a distance-minimising search will never remove. |
| `fleet_mix.py` | Which vans go out, and what the last one is worth. |
| `tactical_sizing.py` | How many vans to own, decided against thirty days instead of one. |
| `territories.py` | What it costs to send the same driver to the same street. |

## `examples/src/fleet/dynamic/`

| Script | Demonstrates |
|---|---|
| `breakdown_at_eleven.py` | A van goes down at 11:00, and most of the day does not move. |
| `churn_tradeoff.py` | What a quiet afternoon is worth, in money. |
| `committed_state.py` | Eleven o'clock, and the morning is not up for discussion. |
| `dispatch_waves.py` | Send it now, or wait and send it with the next three. |
| `insertion_quote.py` | Pricing a stop while the customer is still on the phone. |
| `preemption.py` | A gas escape does not queue behind six routine jobs. |
| `prize_collecting_epoch.py` | Letting the router decide what is worth sending. |
| `replay_policies.py` | Ninety days, three policies, one number each. |
| `sample_scenario_policy.py` | Guessing at tomorrow to decide about this afternoon. |

## `examples/src/fleet/explain/`

| Script | Demonstrates |
|---|---|
| `preflight_diagnosis.py` | Why can't this stop be served? Answer it before solving, and specifically. |
| `why_unassigned.py` | A diagnosis is half an answer. This is the other half. |

## `examples/src/fleet/infra/`

| Script | Demonstrates |
|---|---|
| `accelerator_profile.py` | Proving a GPU is optional, on a machine that has none. |
| `decomposition_queue.py` | Solving the clusters at once, and finding out when that is worth doing. |
| `degraded_matrix.py` | A provider that stops answering should cost you the arcs, not the day. |
| `plan_snapshots.py` | Proving a plan came from the inputs somebody says it came from. |
| `portfolio_parallelism.py` | Running the portfolio wide, and measuring whether it helped. |
| `process_portfolio.py` | Giving a pure-Python engine a core of its own. |
| `run_record.py` | What a run has to be able to tell you afterwards. |

## `examples/src/fleet/learn/`

| Script | Demonstrates |
|---|---|
| `canary_rollout.py` | Deciding in advance what would make you stop. |
| `plan_adherence.py` | When three drivers reverse the same route, the map is wrong. |
| `service_time_calibration.py` | Five minutes on paper, seven in the street. |
| `speed_calibration.py` | Learning the traffic from what the vans actually did. |
| `zone_sequence_prior.py` | Twenty days of drivers leaving San Jose Central until last. |

## `examples/src/fleet/p0/`

| Script | Demonstrates |
|---|---|
| `must_work_at_v1.py` | The fourteen operations that must work at v1, and the obvious wrong answer. |

## `examples/src/fleet/rich/`

| Script | Demonstrates |
|---|---|
| `arc_class_profiles.py` | Rush hour on the ring road is not rush hour on a lane. |
| `departure_scheduling.py` | Two hours of duty that nobody was paying attention to. |
| `engine_portfolio.py` | Two solvers, one scoreboard: why a portfolio must not believe its engines. |
| `ev_recharging.py` | A van that has to stop for electricity, and a plan that knows when. |
| `heterogeneous_fleet.py` | Six depots, mixed vehicles: the shape this business actually has. |
| `hours_of_service.py` | A long-haul day under EU and US driving law, and what the law costs. |
| `large_instance_decomposition.py` | Two thousand stops, fifty sub-problems, and the constraint none of them can see. |
| `locks_and_overrides.py` | Which two of your twelve instructions disagree. |
| `multi_capacity.py` | A van is full when *any* dimension runs out — and totals are the wrong test. |
| `multi_period.py` | Four inspections a year is not four inspections in January. |
| `multi_trip.py` | A second trip the driver has to pay for, and eight bays for forty vans. |
| `planning_under_congestion.py` | Knowing the traffic is not the same as planning around it. |
| `priority_sources.py` | A legal duty, a contract and a preference are not three weights on one scale. |
| `prizes_and_priority.py` | Work worth declining, and work that is not for sale. |
| `ride_time.py` | How long it may be aboard is not the same as when it may be dropped. |
| `set_partitioning_polish.py` | What a search throws away, and whether it is worth picking up. |
| `skills_and_access.py` | Three plans that look perfect and cannot be driven. |
| `synchronisation.py` | The cargo bikes cannot leave before the lorry arrives. |
| `time_dependent.py` | Leaving later must never mean arriving earlier. |

## `examples/src/fleet/tw/`

| Script | Demonstrates |
|---|---|
| `envelope_round.py` | Signed documents: a round where the driving barely matters. |
| `multiple_windows.py` | When can this stop be served, and what does being late actually cost? |
| `sla_windows.py` | Who was promised what, and did the round keep it? |

## `examples/src/fleet/verify/`

| Script | Demonstrates |
|---|---|
| `external_plan.py` | Checking a plan we did not make. |

## `examples/src/infra/`

| Script | Demonstrates |
|---|---|
| `health_and_metrics.py` | Infrastructure Observability Demo |

## `examples/src/routing/`

| Script | Demonstrates |
|---|---|
| `error_handling_demo.py` | Error Handling Demo |
| `match_example.py` |  |
| `matrix_example.py` |  |
| `matrix_graph_example.py` | Matrix-to-Graph Conversion (/matrix-graph) |
| `nearest_example.py` |  |
| `route_advanced_options.py` | Advanced Route Options Demo |
| `tile_example.py` |  |
| `visualize_routes.py` |  |

## `examples/tools/`

| Script | Demonstrates |
|---|---|
| `build_sample_slice.py` | Rebuild the committed corpus slice, and say what it guarantees. |
