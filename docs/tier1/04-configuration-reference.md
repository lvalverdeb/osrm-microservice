<!-- Generated by docs/tier1/build.py. Do not edit by hand; run `make tier1`. -->

# 04 — Configuration reference

*Derived from `gateway/src/config.rs`, `deploy/env/app.env`.*

44 settings. Every one is read by name from the process environment, falls back to the committed default when unset or unparseable, and is matched case-insensitively.

## Settings

| Variable | Group | Type | Default | `app.env` | Agrees |
|---|---|---|---|---|---|
| `OSRM_BASE_URL` | Core | `String` | `http://localhost:5000` | `http://localhost:5000` | yes |
| `OSRM_URL_DRIVING` | Core | `String` | *(empty)* | *(empty)* | yes |
| `OSRM_URL_CYCLING` | Core | `String` | *(empty)* | *(empty)* | yes |
| `OSRM_URL_WALKING` | Core | `String` | *(empty)* | *(empty)* | yes |
| `APP_NAME` | Core | `String` | `OSRM API Gateway` | `OSRM API Gateway` | yes |
| `DEBUG` | Core | `bool` | `false` | `false` | yes |
| `RATE_LIMIT_ROUTE` | Rate limiting | `String` | `600/minute` | `600/minute` | yes |
| `RATE_LIMIT_MATRIX` | Rate limiting | `String` | `300/minute` | `300/minute` | yes |
| `RATE_LIMIT_MATCH` | Rate limiting | `String` | `600/minute` | `600/minute` | yes |
| `RATE_LIMIT_TRIP` | Rate limiting | `String` | `300/minute` | `300/minute` | yes |
| `RATE_LIMIT_VRP` | Rate limiting | `String` | `100/minute` | `100/minute` | yes |
| `RATE_LIMIT_NEAREST` | Rate limiting | `String` | `600/minute` | `600/minute` | yes |
| `RATE_LIMIT_NEAREST_BATCH` | Rate limiting | `String` | `60/minute` | `60/minute` | yes |
| `RATE_LIMIT_TILE` | Rate limiting | `String` | `600/minute` | `600/minute` | yes |
| `REDIS_URL` | L2 cache (Redis) | `String` | *(empty)* | *(empty)* | yes |
| `REDIS_TTL` | L2 cache (Redis) | `u64` | `900` | `900` | yes |
| `L1_CACHE_TTL` | L1 cache (in-process) | `u64` | `900` | `900` | yes |
| `L1_CACHE_MAXSIZE` | L1 cache (in-process) | `u64` | `1024` | `1024` | yes |
| `OTLP_ENDPOINT` | Tracing | `String` | *(empty)* | *(empty)* | yes |
| `OSRM_CLIENT_TIMEOUT` | OSRM client | `u64` | `30` | `30` | yes |
| `OSRM_RETRY_ATTEMPTS` | OSRM client | `usize` | `3` | `3` | yes |
| `OSRM_RETRY_MIN` | OSRM client | `u64` | `1` | `1` | yes |
| `OSRM_RETRY_MAX` | OSRM client | `u64` | `10` | `10` | yes |
| `HEALTH_CHECK_TIMEOUT` | Health check | `u64` | `2` | `2` | yes |
| `HEALTH_CHECK_COORDS` | Health check | `String` | `0,0;0,0` | `0,0;0,0` | yes |
| `VRP_CHUNK_SIZE` | VRP / matrix | `usize` | `80` | `80` | yes |
| `MATRIX_BATCH_SIZE` | VRP / matrix | `usize` | `500` | `500` | yes |
| `VRP_SANITY_LIMIT_M` | VRP / matrix | `f64` | `50000.0` | `50000.0` | yes |
| `VRP_MAX_STOPS` | VRP capacity guards | `usize` | `2000` | `2000` | yes |
| `VRP_MAX_CONCURRENCY` | VRP capacity guards | `usize` | `1` | `1` | yes |
| `VRP_QUEUE_TIMEOUT` | VRP capacity guards | `f64` | `10.0` | `10.0` | yes |
| `VRP_MAX_QUEUE_DEPTH` | VRP capacity guards | `usize` | `0` | `0` | yes |
| `MATRIX_MAX_CELLS` | Matrix capacity | `usize` | `10000` | `10000` | yes |
| `OSRM_MAX_URL_BYTES` | Upstream request size | `usize` | `24000` | `24000` | yes |
| `VRP_CHUNK_CONCURRENCY` | VRP chunk fan-out | `usize` | `4` | `4` | yes |
| `NEAREST_MAX_COORDINATES` | Batch nearest fan-out | `usize` | `1000` | `1000` | yes |
| `NEAREST_BATCH_CONCURRENCY` | Batch nearest fan-out | `usize` | `8` | `8` | yes |
| `API_SUNSET` | API versioning (NFR-10) | `String` | *(empty)* | *(empty)* | yes |
| `METRICS_ENDPOINT` | Metrics | `String` | `/metrics` | `/metrics` | yes |
| `APPEND_TO_STDERR` | Logging | `bool` | `false` | `false` | yes |
| `HOST` | Runtime-only | `String` | `127.0.0.1` | — | yes |
| `PORT` | Runtime-only | `u16` | `8000` | — | yes |
| `WORKERS` | Runtime-only | `usize` | `1` | — | yes |
| `FORWARDED_ALLOW_IPS` | Runtime-only | `String` | *(empty)* | — | yes |

## Why a setting exists

| Variable | Note |
|---|---|
| `OSRM_URL_DRIVING` | MTX-1: "matrices are per routing profile". One `osrm-routed` serves one built graph, so a gateway offering three profiles needs three engines. Empty means this deployment does not serve that profile, and a request for it is refused by name rather than answered from the driving graph -- which is what happened before these existed: `/route` with `profile=walking` returned car routing, byte for byte, with nothing saying so. Driving falls back to `OSRM_BASE_URL` so a single-engine deployment keeps working unchanged. |
| `RATE_LIMIT_NEAREST_BATCH` | Lower than `/nearest` because one request is many engine calls: at the 1,000-coordinate cap, 60 a minute is already 60,000 upstream snaps. |
| `OSRM_MAX_URL_BYTES` | Largest upstream URL the gateway will construct, in bytes. OSRM carries coordinates in the path, so a long `/match` trace or a wide `/matrix` builds a request line of tens of kilobytes. Measured against osrm-routed 5.x directly: it answers up to ~24,750 bytes and drops the connection from ~25,000, which is what turned an over-long `/match` into a 500 rather than any kind of refusal. 24,000 sits under that with a margin and well above the ~13 KB a default VRP `/table` batch builds. Lower it if a proxy sits in front of the engine -- nginx and Apache both cap a request line near 8 KB, far below what osrm-routed itself takes. |
| `NEAREST_MAX_COORDINATES` | Coordinates one `/nearest/batch` may carry. Refused with a 422 past this, because the cost is upstream calls rather than parsing. |
| `NEAREST_BATCH_CONCURRENCY` | Concurrent engine calls one batch may have in flight. The same lever as `VRP_CHUNK_CONCURRENCY` and for the same reason: osrm-routed is shared, and one caller should not be able to take all of it. |
| `API_SUNSET` | When the unversioned paths stop being served, as an HTTP-date. NFR-10 requires a deprecation window, and a window needs an end somebody has committed to. Empty means none has been set: the unversioned surface still announces itself as deprecated and still points at its successor, but no `Sunset` header is sent, because inventing a date here would be this gateway making a promise on an operator's behalf. Example: `Sun, 01 Mar 2026 00:00:00 GMT`. |

## Read outside the settings table

*(none)*

## Carried by `app.env` but deliberately not read

| Variable | Why not |
|---|---|
| `OSRM_API_URL` | declared in config.py but read nowhere in src/ |
| `REDIS_MAXSIZE` | stored on RedisCache and never used; Redis evicts server-side via maxmemory-policy |
| `VRP_HYSTERESIS_M` | shadowed by VrpRequest.hysteresis_m, which hardcodes its own 2000.0 default |
