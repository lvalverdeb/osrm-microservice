<!-- Generated by docs/tier1/build.py. Do not edit by hand; run `make tier1`. -->

# 05 — Domain model reference

*Derived from `vrp/model.py`.*

12 frozen dataclasses. Every one validates itself on construction, so an invalid instance cannot be built and no downstream stage has to re-check it.

## Types

| Type | Fields |
|---|---|
| [`TimeWindow`](#timewindow) | 5 |
| [`Location`](#location) | 9 |
| [`StopSpec`](#stopspec) | 5 |
| [`Order`](#order) | 13 |
| [`Vehicle`](#vehicle) | 28 |
| [`TravelMatrix`](#travelmatrix) | 4 |
| [`Lock`](#lock) | 6 |
| [`Synchronisation`](#synchronisation) | 5 |
| [`Problem`](#problem) | 10 |
| [`Step`](#step) | 10 |
| [`Route`](#route) | 2 |
| [`Solution`](#solution) | 7 |

## Errors

| Exception | Base | Raised when |
|---|---|---|
| `UnreachableArc` | `LookupError` | Raised when travel is read for a pair no route connects. |
| `ValidationError` | `ValueError` | A domain object was constructed in a state the specification forbids. |

## Detail

### TimeWindow

A window a stop may be served in. §4.1.

| Field | Type | Default |
|---|---|---|
| `start` | `int` | **required** |
| `end` | `int` | **required** |
| `hardness` | `str` | `'HARD'` |
| `earliness_cost_per_sec` | `int` | `0` |
| `lateness_cost_per_sec` | `int` | `0` |

Refused on construction:

- end {…} precedes start {…}
- unknown hardness {…}
- {…} is only meaningful on a SOFT window
- {…} must not be negative

### Location

| Field | Type | Default |
|---|---|---|
| `id` | `str` | **required** |
| `lat` | `float` | **required** |
| `lon` | `float` | **required** |
| `matrix_index` | `int` | **required** |
| `dwell_overhead` | `int` | `0` |
| `dock_capacity` | `int \| None` | `None` |
| `inventory` | `dict[str, int] \| None` | `None` |
| `access_classes` | `frozenset[str]` | `frozenset()` |
| `max_vehicle_kg` | `int \| None` | `None` |

Refused on construction:

- dock_capacity must not be negative
- dwell_overhead must not be negative
- inventory[{…}] must not be negative
- latitude {…} out of range
- location id must not be empty
- longitude {…} out of range
- matrix_index must not be negative
- max_vehicle_kg must not be negative

### StopSpec

One end of an order: where it happens, when it may, and for how long.

| Field | Type | Default |
|---|---|---|
| `location_id` | `str` | **required** |
| `time_windows` | `tuple[TimeWindow, ...]` | `()` |
| `service_fixed` | `int` | `0` |
| `service_per_unit` | `int` | `0` |
| `service_per_unit_dimension` | `str \| None` | `None` |

Refused on construction:

- location_id must not be empty
- service_fixed must not be negative
- service_per_unit must not be negative
- time_windows must be sorted and disjoint
- time_windows must hold TimeWindow

### Order

| Field | Type | Default |
|---|---|---|
| `id` | `str` | **required** |
| `kind` | `str` | **required** |
| `quantities` | `dict[str, int]` | `dict()` |
| `pickup` | `StopSpec \| None` | `None` |
| `delivery` | `StopSpec \| None` | `None` |
| `priority_tier` | `int` | `0` |
| `prize` | `int` | `0` |
| `release_time` | `int` | `0` |
| `required_skills` | `frozenset[str]` | `frozenset()` |
| `max_ride_time` | `int \| None` | `None` |
| `priority_source` | `str` | `'COMMERCIAL'` |
| `order_class` | `str \| None` | `None` |
| `incompatible_with` | `frozenset[str]` | `frozenset()` |

Refused on construction:

- a JOB needs exactly one of pickup or delivery
- a SHIPMENT needs both a pickup and a delivery
- a STATUTORY order may not carry a prize: a prize is the price at which declining is acceptable, and there is none
- a maximum ride time must be positive
- an order declaring incompatible_with must have an order_class, or nothing can be incompatible with it in turn
- max_ride_time bounds the time between a pickup and its delivery, so it belongs on a SHIPMENT; a job has one stop
- order id must not be empty
- quantities[{…}] must not be negative
- unknown kind {…}
- unknown priority source {…}; §12.2 names {…}

### Vehicle

| Field | Type | Default |
|---|---|---|
| `id` | `str` | **required** |
| `capacities` | `dict[str, int]` | **required** |
| `shift` | `TimeWindow` | **required** |
| `start_location_id` | `str` | **required** |
| `end_location_id` | `str \| None` | `None` |
| `max_duration` | `int \| None` | `None` |
| `max_distance` | `int \| None` | `None` |
| `skills` | `frozenset[str]` | `frozenset()` |
| `fixed_cost` | `int` | `0` |
| `cost_per_metre` | `int` | `0` |
| `cost_per_second` | `int` | `0` |
| `overtime_cost_per_second` | `int` | `0` |
| `cost_per_order` | `int` | `0` |
| `profile` | `str` | `'driving'` |
| `service_factor_ppt` | `int` | `1000` |
| `reload_locations` | `frozenset[str]` | `frozenset()` |
| `max_reloads` | `int` | `0` |
| `reload_duration` | `int` | `0` |
| `battery_wh` | `int \| None` | `None` |
| `consumption_wh_per_km` | `int` | `0` |
| `charger_locations` | `frozenset[str]` | `frozenset()` |
| `charging_curve` | `ChargingCurve \| None` | `None` |
| `initial_soc_ppt` | `int` | `1000` |
| `access_class` | `str \| None` | `None` |
| `gross_weight_kg` | `int \| None` | `None` |
| `open_route` | `bool` | `False` |
| `hos_rules` | `str \| None` | `None` |
| `initial_state` | `DriverState \| None` | `None` |

Refused on construction:

- battery_wh must be positive
- capacities[{…}] must not be negative
- initial_soc_ppt must be between 0 and {…}
- max_reloads must not be negative
- max_reloads without reload_locations permits reloading nowhere; name the depots or satellites that hold stock
- profile must not be empty
- reload_duration must not be negative
- service_factor_ppt must be positive
- shift must be a TimeWindow
- start_location_id must not be empty
- vehicle id must not be empty
- {…} declares charging but no battery_wh; a vehicle with no battery does not have a range
- {…} has a battery and no charging curve, so how long it spends on a charger is unanswerable
- {…} has a battery and no consumption, which is a vehicle with unlimited range wearing FR-20's fields
- {…} must not be negative

### TravelMatrix

Pinned travel data. `version` is what INV-4 checks a solution against.

| Field | Type | Default |
|---|---|---|
| `version` | `str` | **required** |
| `durations` | `tuple[tuple[int, ...], ...]` | **required** |
| `distances` | `tuple[tuple[int, ...], ...]` | **required** |
| `degraded` | `str \| None` | `None` |

Refused on construction:

- a degraded matrix must say why; an empty reason is a flag pretending to be an explanation
- matrix version must not be empty
- {…} cell {…} is negative and is not UNREACHABLE ({…})
- {…} must be square ({…}x{…})
- {…} must have {…} rows

### Lock

An operator instruction the plan must honour exactly. §6.6, FR-21.

Locks are hard constraints, not preferences. §6.6 is explicit that a lock
set which makes the instance infeasible must be reported with the minimal
conflicting subset rather than quietly relaxed -- because a dropped lock is
a dispatcher's decision being overruled without anyone being told.

| Field | Type | Default |
|---|---|---|
| `kind` | `str` | **required** |
| `order_id` | `str \| None` | `None` |
| `vehicle_id` | `str \| None` | `None` |
| `order_ids` | `tuple[str, ...]` | `()` |
| `depot_id` | `str \| None` | `None` |
| `instant` | `int \| None` | `None` |

Refused on construction:

- unknown lock kind {…}; §6.6 defines {…}
- {…} needs {…}

### Synchronisation

Two routes meeting at a place and a time. FR-26, §12.2.

Every other constraint in this model belongs to something: a capacity to a
vehicle, a window to a stop, an obligation to an order. This one belongs to
a *pair*, and that is what makes it different in kind -- each route can be
beyond reproach on its own while the plan is fiction.

`TRANSFER` is `UC-131`'s two-echelon satellite: "the second-echelon
departure depends on the first echelon's arrival". The second order may not
be served until the first has been, plus whatever the handover takes.

`CONVOY` is `UC-147`: "vehicles must travel in convoy, which is a
synchronisation constraint forcing several routes to share a path and a
schedule". The two are served together, within `max_gap` of each other.

Attributes:
    kind: `TRANSFER` or `CONVOY`.
    first: the order that must happen first, or either of a convoy pair.
    second: the order that follows, or the other half of the convoy.
    min_gap: seconds the second must wait after the first departs. The
        handover itself -- craning a container across, breaking a pallet
        down for cargo bikes.
    max_gap: the longest the coupling tolerates. Required for a convoy,
        where it is how far apart "together" may be; optional for a
        transfer, where it bounds how long goods may sit at the satellite.

| Field | Type | Default |
|---|---|---|
| `kind` | `str` | **required** |
| `first` | `str` | **required** |
| `second` | `str` | **required** |
| `min_gap` | `int` | `0` |
| `max_gap` | `int \| None` | `None` |

Refused on construction:

- a convoy needs a max_gap: 'together' with no bound is not a constraint, and zero is a legitimate value for it
- a handover cannot take negative time
- a synchronisation couples two orders; one order is a sequence
- max_gap must not be shorter than the handover it allows
- unknown synchronisation {…}; FR-26 defines {…}

### Problem

| Field | Type | Default |
|---|---|---|
| `id` | `str` | **required** |
| `locations` | `tuple[Location, ...]` | **required** |
| `orders` | `tuple[Order, ...]` | **required** |
| `vehicles` | `tuple[Vehicle, ...]` | **required** |
| `matrix` | `TravelMatrix` | **required** |
| `horizon` | `TimeWindow \| None` | `None` |
| `locks` | `tuple[Lock, ...]` | `()` |
| `synchronisations` | `tuple[Synchronisation, ...]` | `()` |
| `speed_profile` | `SpeedProfile \| None` | `None` |
| `speed_profiles` | `Mapping[str, SpeedProfile] \| None` | `None` |

Refused on construction:

- duplicate location id
- duplicate matrix_index
- duplicate order id
- lock {…} references unknown order {…}
- lock {…} references unknown vehicle {…}
- matrix_index outside the matrix
- order {…} references unknown location {…}
- problem id must not be empty
- speed_profiles is empty; omit it rather than declaring no profiles, which reads as free flow but is not stated
- synchronisation references unknown order {…}
- the instance declares both a single speed_profile and a per-class speed_profiles mapping; they are alternatives
- the matrix contains {…} arcs and speed_profiles has no profile for them; classes present: {…}
- vehicle {…} references unknown location {…}

### Step

| Field | Type | Default |
|---|---|---|
| `type` | `str` | **required** |
| `location_id` | `str` | **required** |
| `arrival` | `int` | **required** |
| `start_service` | `int` | **required** |
| `departure` | `int` | **required** |
| `order_id` | `str \| None` | `None` |
| `load_after` | `dict[str, int]` | `dict()` |
| `rule_ref` | `str \| None` | `None` |
| `placement` | `str \| None` | `None` |
| `soc_after_ppt` | `int \| None` | `None` |

Refused on construction:

- rule_ref belongs on a BREAK step
- unknown step type {…}

### Route

| Field | Type | Default |
|---|---|---|
| `vehicle_id` | `str` | **required** |
| `steps` | `tuple[Step, ...]` | **required** |

### Solution

| Field | Type | Default |
|---|---|---|
| `problem_id` | `str` | **required** |
| `routes` | `tuple[Route, ...]` | **required** |
| `unassigned` | `tuple[dict[str, Any], ...]` | `()` |
| `objective_breakdown` | `dict[str, int]` | `dict()` |
| `status` | `str` | `'FEASIBLE'` |
| `degraded` | `str \| None` | `None` |
| `solver` | `dict[str, Any] \| None` | `None` |
